{% extends 'base.html.twig' %}

{% block title %}Modifier mon profil{% endblock %}

{% block body %}
    <h2>Modifier mon profil</h2>

    <div class="row">
        <div class="col-sm-4">
            <div id="profile_pic">
                {% if app.user.photo %}
                    <img src="{{ asset('images/avatars'~app.user.photo) }}" alt="{{ app.user.prenom }} {{ app.user.nom }}">
                {% else %}
                    <img src="{{ asset('images/avatars/avataaars(1).png') }}" alt="Photo par défaut">
                {% endif %}
            </div>
            <a href="{{ path('user_upload') }}">Changer ma photo</a><br>
            <a href="{{ path('user_edit_password') }}">Modifier mon mot de passe</a>

        </div>
        <div class="col-sm-8">
            {{ form(registrationForm) }}
        </div>
    </div>
{% endblock %}
