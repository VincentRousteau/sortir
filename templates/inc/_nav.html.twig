{% block navbar %}
        <nav>
            <ul>
                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a href="{{ path('ville_creation') }}">Ville</a>
                    </li>
                    <li>
                        <a href="{{ path("campus_creation") }}">Campus</a>
                    </li>
                {% endif %}
                <li>
                    <a href="{{ path('homepage') }}">Accueil</a>
                </li>
                <li>
                    <a href="{{ path('show_profile') }}">Mon profil</a>
                </li>
                {% if app.user %}
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                {% endif %}
            </ul>
        </nav>
{% endblock %}
