{% block navbar %}
        <nav>
            <ul>
                <li>
                    <a href="{{ path('homepage') }}">Accueil</a>
                </li>
                <li>
                    <a href="{{ path('profile_edit') }}">Mon profil</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a href="{{ path('ville_creation') }}">Ville</a>
                    </li>
                    <li>
                        <a href="{{ path("campus_creation") }}">Campus</a>
                    </li>
                {% endif %}
            </ul>
        </nav>
{% endblock %}
